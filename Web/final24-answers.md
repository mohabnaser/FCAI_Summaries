# 2- Final 2024 - Answers.pdf

**Course Name:** Web Technology
**Course Code:** IS231
**Date:** 06-June-2024
**Exam Duration:** 2 Hours
**Instructor(s):** Dr. Neamat El-Tazi & Dr. Laila Esheiba
**Total Marks:** 60

---
### Final Exam
**PLEASE DELIVER THE EXAM PAPER WITH THE BUBBLE SHEET.**
For each of the following statements, choose the right answer and copy your answer in the bubble sheet:

1. Django is based on which framework?
   a. MVC
   b. MVVM
   c. MVT Model View Template  **(Correct)**
   d. None of the previous

2. For a model to be visible in the admin interface it has to be registered in which file?
   a. settings.py
   b. admin.py (using admin.site.register(Model_name)) **(Correct)**
   c. views.py
   d. None of the previous

3. Which of the following statement(s) is true about the JavaScript?
   a. It is a scripting language used to make the website interactive **(Correct)**
   b. It is an advanced version of Java for Desktop and Mobile application development
   c. It is a markup language of Java to develop the webpages
   d. All of the above

4. To perform programming logic in Django templates, you can use 'template tags', what is a correct syntax to start a 'for' loop?
   a. `(for x in y)`
   b. `<% for x in y %>`
   c. `{% for x in y %}` **(Correct)**
   d. None of the previous

5. What is a correct syntax for creating a Django project?
   a. `django start my_tennis_club`
   b. `py manage.py start-django my_tennis_club`
   c. `django-admin startproject my_tennis_club` **(Correct)**
   d. None of the previous

6. If you have a Django application named members, and a model named Member, what is a correct syntax to import the model?
   a. `from members.models import Member` **(Correct)**
   b. `import Member from members`
   c. `import members.Member`
   d. None of the previous

7. Which is the correct syntax to call an external JavaScript file in the current HTML document?
   a. `<script src="jsfile.js"></script>` **(Correct)**
   b. `<script href=" jsfile.js"></script>`
   c. `<import src=" jsfile.js"></import>`
   d. `<script link=" jsfile.js"></script>`

8. Which JavaScript method is used to access an HTML element by id?
   a. `getElementById()`
   b. `getElement(id)`
   c. `getElementById(id)` **(Correct)**
   d. `elementById(id)`

9. Which property is used to define the HTML content to an HTML element with a specific id?
   a. `innerText`
   b. `innerContent`
   c. `elementText`
   d. `innerHTML` **(Correct)**

10. When you have done changes in a model in Django, which command has to be executed in order to make the changes take effect in the database?
    a. `python manage.py runmigrations`
    b. `python manage.py executemigrations`
    c. `python manage.py makemigrations` **(Correct)**
    d. None of the previous

11. Which JavaScript method is used to write on browser's console?
    a. `console.write()`
    b. `console.output()`
    c. `console.log()` **(Correct)**
    d. `console.writeHTML()`

12. Which JavaScript method is used to write into an alert box?
    a. `window.alertHTML()`
    b. `window.alert()` **(Correct)**
    c. `window.alertBox()`
    d. `window.alertContent()`

13. In HTML DOM, which of the following properties are read-only?
    a. `nodeName`
    b. `nodeValue`
    c. `nodeName` and `nodeValue` **(Correct)**
    d. None of the previous

14. Which is the correct syntax to declare a constant in JavaScript?
    a. `const constant_name;`
    b. `constant name const;`
    c. `constant name const = value;`
    d. `const constant_name = value;` **(Correct)**

15. What will be the output of this code? `const VALUE=10; VALUE=20;`
    a. 10
    b. 20
    c. ValueError
    d. TypeError **(Correct)**

16. Can we use a function as a variable value?
    a. Yes **(Correct)**
    b. No

17. What will be the value of X? `var X=10; function myfunc() { let x=20; document.write(X); }`
    a. 20
    b. 10 **(Correct)**
    c. ValueError
    d. TypeError

18. What is the output of the following JavaScript code? `var a=10+20+"5"; document.getElementById("demo").innerHTML = a;`
    a. 35
    b. 305 **(Correct)**
    c. TypeError
    d. ValueError

19. What will be the output of the following JavaScript code? `var x=12.34; document.getElementById("test").innerHTML = typeof(x);`
    a. int
    b. float
    c. long
    d. number **(Correct)**

20. Which is the correct syntax to access an object property in JavaScript?
    a. `objectName:propertyName`
    b. `objectName[propertyName]`
    c. `objectName["propertyName"]` **(Correct)**
    d. None of the previous

21. What will be the output of the following JavaScript code? `var numbers = [1, 2, 3, 4]; function myfunc(total, value) { return total + value; } var sum = numbers.reduce(myfunc, 2);`
    a. 10
    b. 7
    c. 9
    d. None of the previous **(Correct - output is 12)**

22. What will be the output of the following JavaScript code? `var numbers = [170, 50, 25]; function myfunc(total, value) { return total - value; } var x = numbers.reduce(myfunc); document.write(x);`
    a. 100
    b. 7
    c. 95 **(Correct)**
    d. None of the previous

23. "Expires: Tuesday, 17 Dec 2020 18:30 GMT" is a header denoting:
    a. request, time-based caching
    b. response, content-based caching
    c. request, content-type caching
    d. response, time-based caching **(Correct)**

24. What will be the output of the following JavaScript code? `const arr = [10, 20, 30]; let result = 0; arr.forEach(myFunction); document.write("Result: ", result); function myFunction(value, index, array) { result += value; }`
    a. Result: 102030
    b. Result: 60 **(Correct)**
    c. Result: 10,20,30
    d. ValueError

25. What will be the output of the following JavaScript code? `const values = [10, 20, 30]; const result = values.map(myFunction); function myFunction(value, index, array) { return value * value; }`
    a. Result: 10,20,30
    b. Result: 10,20,30,40
    c. Result: 100,400,900 **(Correct)**
    d. Result: 100400900

26. Which function is used to serialize an object into a JSON string in JavaScript?
    a. `parse()`
    b. `convert()`
    c. `stringify()` **(Correct)**
    d. None of the previous